version: "3.8"

services:
  sqlite_challenge:
    build: .
    container_name: sqlite_challenge
    ports:
      - "5004:5004"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
